# Maze-Solver

A Python-based maze solver that uses the Depth-First Search (DFS) algorithm with recursion and backtracking to find a valid path from a start point to an end point in a grid-based maze. The solving process is visualized step-by-step in the terminal.
